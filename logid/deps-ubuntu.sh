#!/usr/bin/env bash

sudo apt install cmake build-essential libev-dev libudev-dev libconfig-dev libhidapi-dev libevdev-dev libconfig++-dev

# repo
# https://github.com/PixlOne/logiops

# after install, possible requirement
# sudo ln -s /usr/local/lib/libhidpp.so /usr/lib/libhidpp.so
